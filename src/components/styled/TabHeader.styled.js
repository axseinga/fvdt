import styled from "styled-components";

export const StyledTabHeader = styled.div`
    padding: 2rem 2.5rem;
    background-color: #fdc200;
    background: linear-gradient(
        180deg,
        rgba(252, 195, 0, 1) 0%,
        rgba(238, 163, 13, 1) 100%
    );
    color: #ffffff;
    text-shadow: 1px 1px #636363;
    border-radius: 10px;

    font-size: 2rem;
    cursor: pointer;
`;
