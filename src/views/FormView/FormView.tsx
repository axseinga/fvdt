import { StyledFormView } from "./styled/FormView.styled";
import { Form } from "./Form";

export const FormView = () => {
    return (
        <StyledFormView>
            <Form></Form>
        </StyledFormView>
    );
};
