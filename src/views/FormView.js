import { StyledFormView } from "./FormView.styled";
import { Form } from "../components/Form";

export const FormView = () => {
    return (
        <StyledFormView>
            <Form></Form>
        </StyledFormView>
    );
};
